import React from 'react';
import SideBar from '../components/navigation/side-bar';

const Home = (logout) => {
  return (
    <div>
      <SideBar />
      <h1>Home</h1>
    </div>
  );
};

export default Home;
